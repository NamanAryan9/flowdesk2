[phases.setup]
aptPkgs = ["libatomic1"]

[phases.install]
cmds = [
  "npm install",
  "npm install --prefix client --legacy-peer-deps"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
